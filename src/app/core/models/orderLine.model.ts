export interface AdaptedOrderLine {
  nameSize: string;
  quantity: number;
  price: number;
}

export interface OrderLine {
  namePizza: string;
  nameSize: string;
  quantity: number;
  price: number;
}